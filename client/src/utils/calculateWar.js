import statKeyMap from "./statKeyMap"

// Calculate player war
// first calculate the individual components of the war formula
// then calculate the war
// return the war
// war = (battingRuns + baserunningRuns + fieldingRuns + positionalAdjustment + leagueAdjustment + replacementRuns) / runsPerWin

const battingRuns = (playerData, _key) => {
  const wRAA = 
}
